<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/main.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('partials/navbar', {page:''}) %>
    <div class="container mx-auto hero bg-base-200 rounded-lg p-6">
        <div class="hero">
            <div class="<hero-content>">
                <h1><%=set.Theme.name%></h1>
                <p>Below you can find more information about <%=set.Theme.name%></p>
            </div>
        </div>
    </div>
    <tr>
        <td><img class="avatar w-24 h-16 rounded" src=<%=set.img_url%>></td>
        <td><%= set.name %></td>
        <td><a href='/lego/sets?theme=<%=set.Theme.name%>' class="btn-accent rounded"><%= set.Theme.name %></a></td>
        <td><%= set.year %></td>
        <td><%= set.num_parts %></td>
        <td><%= set.set_num %></td>
        <td><a class="btn btn-success" href="/lego/editSet/<%= set.set_num %>">Edit Set</a></td>
        <td><p id="quotable_content"></p><br><p id="quotable_author"></p></td>
        <script>
            document.addEventListener("DOMContentLoaded", async ()=>{
            const response = await fetch('https://quotable.io/random');
            const quote = await response.json()
            document.getElementById('quotable_content').innerHTML = "Quote: "+ quote.content;
            document.getElementById('quotable_author').innerHTML = "Author: " + quote.author;
            });
        </script>
        <br>
    </tr>
</body>
</html>